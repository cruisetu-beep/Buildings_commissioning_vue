/* ═══════════════════════════════════════════════════════════════
   rule-meta.js · 规则详细文档(mock)
   ───────────────────────────────────────────────────────────────
   对应后端 T_ST_CxRuleMeta(规则元数据表)。每条规则:基础信息
   (name / cxRuleId / scope / version) + 算法与原理四小节
   (principle / mathSteps / dataSource / paramBasis),四小节为
   markdown 字符串(含 LaTeX 公式)。后端接口就绪后把 getRuleMeta
   换成接口取数即可。
   注意:JS 字符串中 LaTeX 反斜杠需写成 \\(如 \\frac、\\bar、\\text)。
   ───────────────────────────────────────────────────────────────
   当前仅填充 C01(其余规则待接入)。
   ═══════════════════════════════════════════════════════════════ */

export const RULE_META = {
  C01: {
    name: "同等气象工况下设备存在高低双工况（原规则1）",
    cxRuleId: "CR0016",
    scope: "全部业态",
    version: "v3.1 重构方案",
    principle: `在高度等价的切片工况下（相同作息、相近的显热负荷驱动），空调系统总电耗应维持在单一稳定平台。若呈现出两个显著分离的功耗平台（一高一低），表明设备存在非必要的双工况无序切换。

本重构方案加入时间范围限定，仅筛选建筑实际工作日日间运行时段，完全排除夜间停机对聚类结果的干扰。`,
    mathSteps: `1. 限制计算时间窗为工作日日间运营时段（09:00 - 17:00）。提取对应温度区间内的总电耗特征向量 $\\mathbf{E} = [E_1, E_2, \\dots, E_N]^T$。

2. 将 $\\mathbf{E}$ 聚类为低功耗簇 $C_1$ 与高功耗簇 $C_2$，聚类中心分别为 $\\mu_1$ 和 $\\mu_2$：

    $$\\arg\\min_{\\mathbf{S}} \\sum_{i=1}^{2} \\sum_{E \\in C_i} \\lVert E - \\mu_i \\rVert^2$$

3. 计算单样本轮廓系数并求取全局平均分离度 $\\bar{S}$。当 $\\bar{S} \\ge 0.55$，且两簇均值差值满足 $\\frac{\\mu_2 - \\mu_1}{\\mu_1} \\ge 35\\%$ 时，且 $\\mu_1$ 大于系统待机功率，判定为"双工况异常"。`,
    dataSource: `- 空调系统总电耗（\`U2000\`）逐时电量（表 \`TTBEMS_DataFlow_Result\`）。
- 室外干球温度 $T_{db}$ 逐时实测数据。`,
    paramBasis: `- **等温区间 $\\pm 1.0\\text{°C}$**：工程热力学上，1.5°C 以内的室外干球温差可视为等效显热外负荷，围护结构传热驱动力差异可忽略，确保聚类样本处于同一气象基准。
- **轮廓系数 $\\bar{S} \\ge 0.55$ 与分层差值 $\\ge 35\\%$**：v2.1 版本中 174 栋建筑该规则命中率高达 49%，大量"伪多工况异常"由多联机部分开启、手工临时干预、楼层加班等高频毛刺引起。v3.2 引入轮廓系数硬卡锁，要求两簇在数理空间上呈现清晰断层；35% 阈值进一步过滤人员密度波动，将诊断聚焦于变频器锁定常频、主备泵同启、旁通阀卡死等"阶跃型高能耗"硬故障，保证现场工单转化率。参考文献：[3], [4], [23]。`,
  },
};

/** 取规则详细文档;无则返回 null。 */
export function getRuleMeta(code) {
  return RULE_META[code] || null;
}
